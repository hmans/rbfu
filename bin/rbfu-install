#!/usr/bin/env bash

VERSION=$1

command_exists () { type "$1" &> /dev/null; }

if command_exists ruby-build; then
  CMD="ruby-build $VERSION $HOME/.rbfu/rubies/$VERSION"
  $CMD
else
  echo "Please install ruby-build: https://github.com/sstephenson/ruby-build"
  exit 1
fi
